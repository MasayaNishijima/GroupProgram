#include <stdio.h>
#include <string.h>
#include "Formula.h"
#include "Training.h"
int i;//選択肢を選ばせるよう

int main(void)
{
	//OPNI();  //最初の画面
	getchar();
	//VSSE()     //対戦相手をどれにするか表示する
	for (;;) {
		scanf("%d", &i);
		if (i == 1) {

		}
		else if (i == 2) {

		}
		else if (i == 3) {

		}
		else if (i == 0) {
	//		END()  //終了 
			break;
		}
	}
	return 0;
}